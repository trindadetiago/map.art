{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "pnpm --filter @mapart/web build"
  },
  "deploy": {
    "startCommand": "pnpm --filter @mapart/web exec next start --port $PORT --hostname 0.0.0.0",
    "preDeployCommand": "pnpm --filter @mapart/db exec drizzle-kit migrate",
    "healthcheckPath": "/api/health",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
